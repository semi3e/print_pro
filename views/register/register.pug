extends ../partials/layout

block content
    .main.container
        .row
            .col-md-6.col-md-offset-3
            h1.display-4.m-b-2 회원가입
            
            // 회원가입 form
            from(method='POST' action='/register')
                div.form-group
                    label(for='student_id') 이름:
                    input#name.form-control(type='text', placeholder='first and last' name='student_id')
                div.form-group
                    label(for='email') 이메일:
                    input#email.form-control(type='email', placeholder='name@email.com' name='email')
                div.form-group
                    label(for='password') 비밀번호:
                    input#password.form-control(type='password' name='password')
                div.form-group
                    label(for='confirmpassword') 비밀번호 확인:
                    input#confirmpassword.form-control(type='password' name='confirmpassword')
                button.btn.btn-primary(type='submit') 회원가입